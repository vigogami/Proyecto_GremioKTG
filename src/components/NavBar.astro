<header class="navbar">
  <div class="container nav-wrapper">
    <div class="brand">
      <img src="/images/logo_ktg.png" alt="Gremio KTG" loading="lazy" />
      <span>Gremio KTG</span>
    </div>
    <nav aria-label="Secciones principales">
      <ul>
        <li><a href="#guia">Guías</a></li>
        <li><a href="#noticias">Noticias</a></li>
        <li><a href="#popular">Populares</a></li>
        <li><a href="#comunidad">Comunidad</a></li>
      </ul>
    </nav>
  </div>
</header>

<style>
  .navbar {
    background: rgba(14, 14, 18, 0.9);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .nav-wrapper {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.75rem 0;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-weight: 600;
    font-size: 1.1rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .brand img {
    width: 42px;
    height: 42px;
    border-radius: 12px;
    object-fit: cover;
  }

  nav ul {
    list-style: none;
    display: flex;
    gap: 1.25rem;
  }

  nav a {
    font-weight: 500;
    color: var(--muted);
    transition: color 0.2s ease;
  }

  nav a:hover,
  nav a:focus-visible {
    color: white;
  }

  @media (max-width: 768px) {
    nav ul {
      gap: 0.75rem;
      font-size: 0.92rem;
    }
  }
</style>
